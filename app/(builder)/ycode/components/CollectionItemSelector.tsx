'use client';

/**
 * Stub for the deleted CMS CollectionItemSelector component.
 */
import React from 'react';

export function CollectionItemSelector(): React.ReactElement {
  return <div className="text-xs text-muted-foreground">Collection items are no longer available.</div>;
}

export default CollectionItemSelector;
