'use client';

/**
 * Stub for the deleted CMS CollectionFiltersSettings component.
 */
import React from 'react';

export function CollectionFiltersSettings(): React.ReactElement {
  return <div className="text-xs text-muted-foreground">CMS filters are no longer available.</div>;
}

export default CollectionFiltersSettings;
