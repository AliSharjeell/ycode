'use client';

/**
 * Stub for the deleted Supabase live layer updates hook. No-op.
 */
export function useLiveLayerUpdates(_pageId: string | null): void {
  // no-op — desktop app has no realtime sync
}
